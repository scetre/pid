from setuptools import setup, find_packages

setup(
    name='pid',
    version='0.1',
    description='A simple PID controller implementation',
    packages=find_packages(),
    install_requires=[],
)
